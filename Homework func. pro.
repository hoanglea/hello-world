#include <stdio.h>
int main()
{
	int i = 0;
	int current=1;
	int previous=0;

	for(i=0; i<20; i++)
	{
	    printf ("%d \n", current);
	    int i= current + previous;
	    previous = current;
	    current = i;
	}
return 0;
}
